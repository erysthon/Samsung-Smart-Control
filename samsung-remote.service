[Unit]
Description=Samsung Remote Mapper Automático
# Note que não precisamos de 'After=bluetooth' aqui, pois o udev só o chama quando o BT já conectou

[Service]
Type=simple
ExecStart=/usr/bin/python /usr/local/bin/samsung_remap.py
Restart=on-failure
# Garante que o script tenha permissão para criar teclas virtuais
User=root

[Install]
WantedBy=multi-user.target
